; Define can be used to control processes

; Run Parameters 
define    = -DPOSRES  ; position restrain the protein

integrator = md         ; leap frog integrator
dt = 0.002              ; 2 fs timestep
nsteps = 1000000000          ; 2us

; Output control
; Output Control
;nstxout   =2000         ; save coordinates every 1 ps
;nstvout   =2000         ; save velocities everyg 4 ps
nstenergy = 2000        ; save energies every 4 ps
nstlog    = 2000         ; update log file every 4 ps
nstxout-compressed = 2000 ; save compressed

compressed-x-grps = System ;

; Bond parameters
continuation = yes       ; continue from NPT position restraint
constraint_algorithm = lincs 
constraints = h-bonds ;h-bonds constrained
lincs_iter  = 1
lincs_order = 4

; Neighbor Lists
cutoff-scheme = Verlet
nstlist = 10             ; Frequency to update the neighbor list 10 fs
ns_type = grid          ; Method to determine neighbor list (simple, grid)
rcoulomb = 1.0          ; long range electrostatic cut-off
rvdw = 1.0              ; long range Van der Waals cut-off

; Coulomb interactions
coulombtype = PME       ; Particle Mesh Ewald 
fourierspacing = 0.16   ; grid spacing for FFT
pme_order = 4           ; cubic interpolation

; Temperature coupling
tcoupl = v-rescale      ; modified Berendsen thermostat
tc-grps = Protein Non-Protein
tau_t = 0.1 0.1         ; time constant in ps
ref_t = 300 300         ; reference temperature, one for each group, in K

; Pressure coupling is on
Pcoupl = Parrinello-Rahman             ; pressure coupling (NPT)
Pcoupltype = Isotropic
tau_p = 2.0
compressibility = 4.5e-5
ref_p = 1.0
refcoord_scaling = com

; Periodic boundary conditions
pbc = xyz               ; Periodic Boundary Conditions (yes/no)

; Dispersion correction
DispCorr = EnerPres     ; account for cut-off vdW scheme


